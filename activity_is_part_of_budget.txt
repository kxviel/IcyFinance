// activity_is_part_of_budget.txt

BEGIN

  INSERT INTO budget_activity(activity_title, activity_amount, budget_id)
  VALUES (activity_title, activity_amount, budget_id);

  UPDATE budget
  SET 
    activity = activity + activity_amount,
    amount_available = assigned_budget - (activity + activity_amount)
  WHERE id = budget_id;

END;